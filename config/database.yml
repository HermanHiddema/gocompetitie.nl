# PostgreSQL. Versions 9.5 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On macOS with Homebrew:
#   gem install pg -- --with-pg-config=/opt/homebrew/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem "pg"
#
<%
require "uri"

production_build = ENV.key?("SECRET_KEY_BASE_DUMMY")

fetch_production_env = lambda do |name, build_default|
  if ENV.key?(name)
    ENV.fetch(name)
  elsif production_build
    build_default
  else
    ENV.fetch(name)
  end
end

database_url_for = lambda do |env_name, suffix|
  if ENV.key?(env_name)
    ENV.fetch(env_name)
  elsif ENV["DATABASE_URL"]
    uri = URI.parse(ENV.fetch("DATABASE_URL")).dup
    uri.path = "#{uri.path}#{suffix}"
    uri.to_s
  end
end
%>

default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see Rails configuration guide
  # https://guides.rubyonrails.org/configuring.html#database-pooling
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS", 3) %>
  <% if ENV["DB_HOST"] %>
  host: <%= ENV["DB_HOST"] %>
  username: <%= ENV.fetch("DB_USERNAME") %>
  password: <%= ENV.fetch("DB_PASSWORD") %>
  <% end %>


development:
  <<: *default
  database: go_competitie_development

  # The specified database role being used to connect to PostgreSQL.
  # To create additional roles in PostgreSQL see `$ createuser --help`.
  # When left blank, PostgreSQL will use the default role. This is
  # the same name as the operating system user running Rails.
  #username: go_competitie

  # The password associated with the PostgreSQL role (username).
  #password:

  # Connect on a TCP socket. Omitted by default since the client uses a
  # domain socket that doesn't need configuration. Windows does not have
  # domain sockets, so uncomment these lines.
  #host: localhost

  # The TCP port the server listens on. Defaults to 5432.
  # If your server runs on a different port number, change accordingly.
  #port: 5432

  # Schema search path. The server defaults to $user,public
  #schema_search_path: myapp,sharedapp,public

  # Minimum log levels, in increasing order:
  #   debug5, debug4, debug3, debug2, debug1,
  #   log, notice, warning, error, fatal, and panic
  # Defaults to warning.
  #min_messages: notice

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: go_competitie_test

# As with config/credentials.yml, you never want to store sensitive information,
# like your database password, in your source code. If your source code is
# ever seen by anyone, they now have access to your database.
#
# Instead, provide the password or a full connection URL as an environment
# variable when you boot the app. For example:
#
#   DATABASE_URL="postgres://myuser:mypass@localhost/somedatabase"
#
# If the connection URL is provided in the special DATABASE_URL environment
# variable, Rails will automatically merge its configuration values on top of
# the values provided in this file. Alternatively, you can specify a connection
# URL environment variable explicitly:
#
#   production:
#     url: <%= ENV["MY_APP_DATABASE_URL"] %>
#
# Connection URLs for non-primary databases can also be configured using
# environment variables. The variable name is formed by concatenating the
# connection name with `_DATABASE_URL`. For example:
#
#   CACHE_DATABASE_URL="postgres://cacheuser:cachepass@localhost/cachedatabase"
#
# Read https://guides.rubyonrails.org/configuring.html#configuring-a-database
# for a full overview on how database connection configuration can be specified.
#
production:
  primary:
    <<: *default
    <% if ENV["RAILS_ENV"] == "production" %>
    <% if ENV["DATABASE_URL"] %>
    url: <%= ENV["DATABASE_URL"] %>
    <% else %>
    host: <%= fetch_production_env.call("DB_HOST", "localhost") %>
    database: <%= fetch_production_env.call("DB_NAME", "go_competitie_production") %>
    username: <%= fetch_production_env.call("DB_USERNAME", "postgres") %>
    password: <%= fetch_production_env.call("DB_PASSWORD", "postgres") %>
    <% end %>
    <% end %>
  cache:
    <<: *default
    <% if ENV["RAILS_ENV"] == "production" %>
    <% if (cache_database_url = database_url_for.call("CACHE_DATABASE_URL", "_cache")) %>
    url: <%= cache_database_url %>
    <% else %>
    host: <%= fetch_production_env.call("DB_HOST", "localhost") %>
    database: <%= "#{fetch_production_env.call("DB_NAME", "go_competitie_production")}_cache" %>
    username: <%= fetch_production_env.call("DB_USERNAME", "postgres") %>
    password: <%= fetch_production_env.call("DB_PASSWORD", "postgres") %>
    <% end %>
    <% end %>
    migrations_paths: db/cache_migrate
  queue:
    <<: *default
    <% if ENV["RAILS_ENV"] == "production" %>
    <% if (queue_database_url = database_url_for.call("QUEUE_DATABASE_URL", "_queue")) %>
    url: <%= queue_database_url %>
    <% else %>
    host: <%= fetch_production_env.call("DB_HOST", "localhost") %>
    database: <%= "#{fetch_production_env.call("DB_NAME", "go_competitie_production")}_queue" %>
    username: <%= fetch_production_env.call("DB_USERNAME", "postgres") %>
    password: <%= fetch_production_env.call("DB_PASSWORD", "postgres") %>
    <% end %>
    <% end %>
    migrations_paths: db/queue_migrate
  cable:
    <<: *default
    <% if ENV["RAILS_ENV"] == "production" %>
    <% if (cable_database_url = database_url_for.call("CABLE_DATABASE_URL", "_cable")) %>
    url: <%= cable_database_url %>
    <% else %>
    host: <%= fetch_production_env.call("DB_HOST", "localhost") %>
    database: <%= "#{fetch_production_env.call("DB_NAME", "go_competitie_production")}_cable" %>
    username: <%= fetch_production_env.call("DB_USERNAME", "postgres") %>
    password: <%= fetch_production_env.call("DB_PASSWORD", "postgres") %>
    <% end %>
    <% end %>
    migrations_paths: db/cable_migrate
