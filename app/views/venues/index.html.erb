<% content_for :title, "Speellokaties" %>
<h1 class="mb-6 text-2xl font-bold">Speellokaties</h1>

<section class="rounded-lg bg-white p-4 shadow-sm ring-1 ring-slate-200">
  <%= render @venues %>
</section>

<% if current_user %>
  <div class="mt-6">
    <%= button_link_to "Lokatie toevoegen", new_venue_path %>
  </div>
<% end %>
