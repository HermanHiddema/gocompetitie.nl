<% content_for :title, "Speellocaties" %>
<h1 class="mb-6 text-2xl font-bold">Speellocaties</h1>

<section class="rounded-lg bg-white p-4 shadow-sm ring-1 ring-slate-200">
  <%= render @venues %>
</section>

<% if current_user %>
  <div class="mt-6">
    <%= button_link_to "Locatie toevoegen", new_venue_path(**season_url_options) %>
  </div>
<% end %>
