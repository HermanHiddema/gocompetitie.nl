<% content_for :title, "Personen" %>
<%= page_header "Personen" do %>
  <% if admin? %>
    <%= button_link_to "Persoon toevoegen", new_person_path, short: "+ Persoon" %>
  <% end %>
<% end %>

<section class="rounded-lg bg-white p-4 shadow-sm ring-1 ring-slate-200">
  <%= render @people %>
</section>
