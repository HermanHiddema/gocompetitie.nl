<% content_for :title, "Personen" %>
<h1 class="mb-6 text-2xl font-bold">Personen</h1>

<section class="rounded-lg bg-white p-4 shadow-sm ring-1 ring-slate-200">
  <%= render @people %>
</section>

<% if current_user %>
  <div class="mt-6">
    <%= button_link_to "Persoon toevoegen", new_person_path %>
  </div>
<% end %>
