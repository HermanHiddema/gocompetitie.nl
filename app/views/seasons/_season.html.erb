<% statistics = season.statistics %>
<div class="grid grid-cols-2 gap-x-4 gap-y-1 border-b border-slate-100 py-2 text-sm last:border-0 md:grid-cols-8">
  <div class="font-medium text-slate-600 md:col-span-2"><%= season.champion&.name %></div>
  <div class="md:col-span-2">
    <%= link_to season.name, season, class: "font-medium hover:underline" %>
    <%= season_phase_badge(season, class: "ml-1 text-xs") %>
  </div>
  <div class="text-slate-600"><%= pluralize(statistics[:leagues], "poule", "poules") %></div>
  <div class="text-slate-600"><%= pluralize(statistics[:clubs], "club", "clubs") %></div>
  <div class="text-slate-600"><%= pluralize(statistics[:teams], "team", "teams") %></div>
  <div class="text-slate-600"><%= pluralize(statistics[:participants], "speler", "spelers") %></div>
</div>
