<% content_for :title, "Teams" %>
<h1 class="mb-6 text-2xl font-bold">Teams</h1>

<section class="rounded-lg bg-white p-4 shadow-sm ring-1 ring-slate-200">
  <%= render @teams %>
</section>

<% if current_user %>
  <div class="mt-6">
    <%= button_link_to "Team toevoegen", new_team_path %>
  </div>
<% end %>
