<% content_for :title, "Teams" %>
<%= page_header "Teams" do %>
  <% if admin? && @season&.draft? %>
    <%= button_link_to "Team toevoegen", new_team_path(**season_url_options), short: "+ Team" %>
  <% end %>
<% end %>

<section class="rounded-lg bg-white p-4 shadow-sm ring-1 ring-slate-200">
  <%= render @teams %>
</section>
